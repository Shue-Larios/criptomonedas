// este archivo es para agregar fuentes personalizadas tiene que se llamado asi como este
module.exports = {
    project: {
        ios: {},
        android: {}
    },
    // ubicacion de las fuentes descargadas dspues de hacer esto hay que reiniciar el emulador
    assets:['./assets/fonts']
}